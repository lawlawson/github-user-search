import styles from './TitleBar.module.css';

const TitleBar = () => {
  return (
    <div className={styles.title}>
      <div>devFinder</div>
      <div>Dark</div>
    </div>
  );
};

export default TitleBar;
